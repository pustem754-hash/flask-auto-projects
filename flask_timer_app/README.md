# Flask Timer App

## Описание
Веб-приложение на Flask с интерактивным таймером. Позволяет устанавливать время в минутах и секундах, запускать/останавливать таймер с визуальным отображением обратного отсчета.

## Возможности
- ⏰ Установка времени таймера (минуты и секунды)
- ▶️ Запуск таймера
- ⏹️ Остановка таймера
- 📊 Визуальное отображение обратного отсчета
- 🔔 Уведомление при завершении
- 📱 Адаптивный дизайн

## Установка и запуск

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Запуск приложения
```bash
python app.py
```

### 3. Открыть в браузере
```
http://localhost:5000
```

## Структура проекта
```
flask_timer_app/
├── app.py # Основное Flask приложение
├── requirements.txt # Зависимости
├── templates/
│ └── index.html # HTML шаблон
├── static/
│ └── style.css # Дополнительные стили
└── README.md # Документация
```

## API Endpoints

### GET /
Главная страница с интерфейсом таймера

### POST /start_timer
Запуск таймера
```json
{
 "minutes": 5,
 "seconds": 30
}
```

### POST /stop_timer
Остановка таймера

### GET /get_status
Получение текущего состояния таймера
```json
{
 "active": true,
 "remaining": 330,
 "duration": 330,
 "finished": false
}
```

## Технологии
- **Backend**: Python + Flask
- **Frontend**: HTML5, CSS3, JavaScript
- **Многопоточность**: Python threading для фонового обновления таймера

## Особенности реализации
- Фоновый поток для обновления состояния таймера
- AJAX запросы для динамического обновления интерфейса
- Responsive дизайн с CSS3 анимациями
- Валидация пользовательского ввода

## Развитие проекта
Возможные улучшения:
- Сохранение истории таймеров
- Звуковые уведомления
- Предустановленные шаблоны времени
- Темная тема
- Несколько параллельных таймеров